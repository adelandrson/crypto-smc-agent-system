[pytest]
; Kept inside tests/ so pytest's rootdir is this directory and it never
; ascends into the plugin root (whose __init__.py is a Hermes package, not a
; test package). The engine is loaded explicitly in conftest.py.
addopts = --import-mode=importlib
